# Copy to .env for local Docker Compose use. Never commit the real secret.
SEWTEC_JWT_KEY=replace-with-a-cryptographically-random-secret-of-at-least-32-bytes
SEWTEC_ALLOWED_HOSTS=localhost;127.0.0.1;backend
